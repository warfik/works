package com.telran;

public class ListTester {
    listTester(listOperationsStrategy Strategy)
long testAppend(nTimes, length);
long testGet (nTimes, length);
}
interface ListOperationsStrategy{

    append(Object[] objects);
        get(nTimes);
}
